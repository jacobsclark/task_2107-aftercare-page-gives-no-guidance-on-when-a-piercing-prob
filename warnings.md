# Warnings

Advisory findings from the gates. NONE of these blocked this task — they are recorded so a reviewer can see what was nearly wrong without having to re-run anything. An empty or absent file means no gate raised a concern.

## Gate A test-quality lint (advisory)

- '\[F2P\] day-selection options wrap onto multiple rows and stay i' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.

## Gate A test-quality lint (advisory)

- '\[F2P\] day-selection options wrap onto multiple rows and stay i' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.

## Gate A test-quality lint (advisory)

- '\[F2P\] day-selection options wrap onto multiple rows and stay i' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.

## Gate A test-quality lint (advisory)

- '\[F2P\] doctor-level urgent callout renders with warning signs a' compares against hand-typed toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] both urgent callouts keep their warning signs for every ' compares against hand-typed toBeGreaterThanOrEqual(3) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] day-since-appointment options wrap onto multiple rows in' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.

## Gate A test-quality lint (advisory)

- '\[F2P\] doctor-level urgent callout renders with warning signs a' compares against hand-typed toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] both urgent callouts keep their warning signs for every ' compares against hand-typed toBeGreaterThanOrEqual(3) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] day-since-appointment options wrap onto multiple rows in' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.

## Gate A test-quality lint (advisory)

- '\[F2P\] doctor-level urgent callout renders with warning signs a' compares against hand-typed toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] both urgent callouts keep their warning signs for every ' compares against hand-typed toBeGreaterThanOrEqual(3) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] day-since-appointment options wrap onto multiple rows in' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.

## Gate A test-quality lint (advisory)

- '\[F2P\] doctor-level urgent callout renders with warning signs a' compares against hand-typed toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] both urgent callouts keep their warning signs for every ' compares against hand-typed toBeGreaterThanOrEqual(3) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.
- '\[F2P\] day-since-appointment options wrap onto multiple rows in' compares against hand-typed toBeGreaterThanOrEqual(3), toBeGreaterThanOrEqual(2) — a solver can return those literals and pass without fixing anything. Seed them randomly and read them from the fixture, or assert a relationship that holds for every input.